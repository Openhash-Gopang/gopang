# JURISDICTION-RESOLVER-SCHEMA
# ═══════════════════════════════════════════════════
# 문서명    : 관할 계층 판별기 — 국가/광역시도/시군구/읍면동 첫 분기
# 문서 코드  : JURISDICTION-RESOLVER-SCHEMA
# 버전      : v1.2
# 근거      : 2026-07-08 주피터님 지시 — "가장 중요한 요소는 첫 단추로서,
#             국가·광역시도·시군구·읍면동 중 어느 도메인으로 사용자 요청을
#             분기할지 결정하는 단계"다. 지금까지 SP-AUTHOR PHASE A는
#             "제주도청 산하인가/국가기관인가"만 얕게 갈랐을 뿐, 실제
#             행정법상 사무 배분 기준(자치사무·위임사무·국가사무)을
#             반영하지 않았고, "접수 창구"와 "실질 처리 주체"가 다를 수
#             있다는 것도 emd 계층 데이터에만 암묵적으로 있었다
#             (05-emd/emd-master-data.json의 접수전용업무/실질처리주체/
#             연결SP 필드, 42개 읍면동 레코드 전체에 이미 존재).
#             v1.1(2026-07-08): 사고실험 4(세무서-AI·시청-AI 세액 상이)
#             결과 반영 — §4(복수 substantive_tier 충돌 시 처리) 신설.
#             v1.2(2026-07-08): 잔여 B급(B-10·B-12) 일괄 반영 — 경찰
#             (국가/자치)·정부산하기관 판별 각주 신설. entity_type=org
#             전용 판별 규칙(목적사업 기준) 신설.
# 적용 대상  : SP-AUTHOR(초안 작성 전 tier_hint 확정), 사용자 AI 비서
#             (실시간 요청 라우팅 — 둘 다 이 문서 하나를 공유 사용)
# 작성일     : 2026-07-08
# 작성자     : AI City Inc. · 주피터
# ═══════════════════════════════════════════════════
#
# 버전 변경 이력
# ─────────────────────────────────────────────────
# v1.2 (2026-07-08): 경찰·정부산하기관 판별 각주, org 전용 판별 규칙 신설.
# v1.1 (2026-07-08): §4(복수 substantive_tier 충돌) 신설.
# v1.0 (2026-07-08): 최초 제정.
# ─────────────────────────────────────────────────

## 원칙 — "어디로 갈지"는 "무엇을 할 수 있는지"보다 먼저 결정된다

지금까지 SP-AUTHOR PHASE A(분류)와 PHASE B(템플릿 조회)는 tier가 이미
주어졌다고 가정하고 시작했다. 실제로는 tier 자체를 결정하는 게 가장 먼저
풀어야 할 문제이자 가장 자주 틀리는 지점이다 — "집 앞 상수도 파열"과
"소송"은 둘 다 "정부에 뭔가 요청한다"는 표면 형태가 같지만 계층이
정반대다. 이 판별을 SP-AUTHOR와 사용자 AI 비서가 각자 다른 기준으로
하면 반드시 드리프트가 생기므로, 이 문서 하나를 양쪽이 공유한다.

## 1단계 — 사무 성격 판별 (행정법상 사무 배분 기준)

| 사무 유형 | 판별 기준 | tier |
|---|---|---|
| 국가사무 | 사법(재판·기소·수사 중 국가경찰 사무)·외교·국방·병무·국세·전국단위 등록/인증 기관 소관 | `national`(제주 소재 사무소 있으면) 또는 `kgov`(없으면) |
| 시·도 사무 | 광역 단위 계획·정책, 제주도 조례로 도가 직접 수행 | `do` (do-dept) |
| 시·군·구 사무 | 대다수의 인허가·영업신고·등록 — 실질 처리권한이 시청에 있는 것 다수 | `city` |
| 읍·면·동 사무 | 제증명 발급, 1차 민원 접수, 기초생활 접수 등 | `emd` |

키워드만으로 기계적으로 정확히 가르기 어려운 경우(대부분의 실제 요청)가
많으므로, 이 표는 절대 기준이 아니라 **2단계로 넘어가기 전의 1차 후보
압축**용이다. "소송" 같은 명백한 예는 표만으로 즉시 `kgov`(사법부는
제주도지사 지휘·감독 밖 — `JEJU-NATIONAL-SP` §1 원칙 그대로)로 확정된다.

**세부 판별 각주** (B-12 검증, #18·#20 — 굵은 범주 안에서 더 흔히
틀리는 경계):

- **경찰**: 국가경찰(형사·수사 등 대부분의 치안사무)은 `national`/`kgov`,
  **제주자치경찰단**(생활안전·교통 등 조례로 위임된 사무에 한함)은
  `do` 소관이다 — "경찰"이라는 단어만으로 판별하지 않는다.
- **정부산하기관(준정부기관)**: 근로복지공단·건강보험공단처럼 법률로
  설립되고 국가사무를 위탁수행하는 기관은 `entity_type=institution`이
  아니라 **정부산하기관**으로 별도 분류하되(PHASE A 기존 분류 유지),
  tier는 그 기관이 위탁받은 사무의 성격을 따른다(대부분 `national`) —
  기관 자체의 법적 형태(공단·공사 등)가 아니라 **위탁받은 사무의
  성격**이 tier를 결정한다.

**`entity_type=org`(협회·비영리) 전용 규칙** (B-10 검증, #44 비영리법인
설립허가): business의 KSIC 같은 표준 분류가 org에는 없다. org의 관할은
법인 정관상 **목적사업의 성격**으로 결정한다(문화 목적 → 문화체육관광부
계열, 복지 목적 → 보건복지부 계열, 환경 목적 → 환경부 계열 등) — 이
매핑을 위한 전용 코드 체계(`org_purpose_code`)는 아직 없으며, 지금은
PHASE C(웹검색)로 건별 판별한다. 향후 org 설립허가 사례가 누적되면
KSIC과 동일한 역할의 표준 코드 체계 신설을 검토한다(지금은 범위 밖).

## 2단계 — 접수 계층과 실질처리 계층은 다를 수 있다

`05-emd/emd-master-data.json`이 이미 42개 읍면동 전체에서 증명한 패턴을
전 계층 공통 어휘로 승격한다:

```json
{
  "intake_tier": "emd",
  "intake_agency": "{읍면동사무소}",
  "substantive_tier": "do",
  "substantive_agency": "제주도 상하수도본부",
  "connected_sp": "SP-EXP-WATER"
}
```

"집 앞 상수도 파열"은 `intake_tier=emd`(신고 접수는 동네 창구)이지만
`substantive_tier=do`(실제 처리는 도 직속 상하수도본부)다 — 둘 다 정답이고,
사용자에게는 `intake_tier`로 응대를 시작하되 처리 주체는 `connected_sp`로
투명하게 안내한다(이미 `emd-master-data.json`이 하고 있는 것과 동일).
이 이원구조를 `do`·`city` 레코드에도 동일 스키마로 확장한다 — 지금까지는
emd에만 있었지만, 시·도 사무 중에도 접수는 시군구가 하고 처리는 도가
하는 경우(반대의 경우도)가 있으므로 계층에 상관없이 이 두 필드 쌍을
표준으로 삼는다.

## 3단계 — city/emd tier의 domain 분기 부재 해소

지난 사고실험에서 발견된 공백: `do-dept`/`national`은 domain별로 레코드가
갈라져 있는데 `city`는 시청 전체 단일 템플릿, `emd`는 읍면동별 단일
레코드(내부에 팀 구성이 배열로만 존재)다. 이 문서가 요구하는 판별을
실제로 수행하려면 `city`·`emd`도 do-dept와 같은 **domain 키 분기**가
필요하다:

```
city-master-data.json  : { domain: "동물미용업등록", 시코드: "jejusi", ... }
emd-master-data.json   : { domain: "상수도", emd_code: "SP-EMD-AEWOL", ... }
```

기존 `emd-master-data.json`의 `접수전용업무` 배열은 이 domain 분기의
원형이었다 — 이번 개정은 그 배열 항목 각각을 독립 레코드로 승격해
`jeju-router.js`의 `_assemble()`이 do-dept와 동일한 방식으로 city/emd도
조회할 수 있게 하는 것이다(스키마 확장이며, 기존 데이터 재입력이
필요한 별도 작업 — 이번 문서는 스키마만 확정).

## 4. 복수 `substantive_tier` 충돌 — 하나로 합치지 않는다

같은 표면 질문("세금 얼마예요?")에 두 개의 `substantive_tier`가 동시에
정답인 경우가 있다(국세=national, 지방세=do/city — 실제로 이 프로젝트의
위치기반서비스 사례에서도 등록면허세를 두고 반복 확인이 필요했다). 이건
tier 판별이 틀린 게 아니라 **애초에 두 기관이 각자 다른 부분에 대해
둘 다 옳은 경우**이므로, 하나를 골라 답하거나 둘을 합산해 대신 답하지
않는다:

```
[JURISDICTION_MULTI: candidates=[
  {substantive_tier, substantive_agency, scope_note},
  {substantive_tier, substantive_agency, scope_note}
], merge=false]
```

`merge=false`가 기본값이자 원칙이다 — 사용자 AI 비서는 각 후보에
개별적으로 `GOV_TASK_REQUEST`(또는 개인정보 없는 단발 질의면 U9)를 보내,
각 기관이 답한 값을 **출처를 밝혀 나란히** 제시한다("국세청 확인 결과
{A}, 제주시 확인 결과 {B}"). 시스템이 대신 판단해 하나만 보여주거나 둘을
더해 확정 금액처럼 제시하지 않는다 — 그 합산·선택은 U2(사실 정확성) 원칙상
사용자 스스로 확인해야 할 몫이다.

## SP-AUTHOR와의 관계 — 새 PHASE 0

```
PHASE 0. 관할 계층 판별 (신설, PHASE A보다 먼저)
  1단계·2단계를 실행해 tier_hint 대신 {intake_tier, substantive_tier,
  substantive_agency, connected_sp}를 확정한다. 이후 PHASE A(개인/조직
  분기)·PHASE B-0(입출력 스키마)은 intake_tier 기준으로 진행하되,
  substantive_tier가 다르면 §CAPABILITIES에 반드시 그 사실을
  `mode: intake_only`로 명시한다(HUMAN-AUTHORITY-GATE-SCHEMA의
  CAPABILITIES 어휘 그대로 재사용 — 새 어휘를 만들지 않는다).
```
