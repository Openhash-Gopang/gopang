# 작업 계획서 (2026-07-11 이어서) — ★ 실행 완료, 결과 반영판

> 아래 원래 계획 본문은 그대로 두고, 실행 중 나온 **추가 정정 사항**을 맨 위에 먼저 적습니다.

## 실행 결과 요약

1. **§0-1 (worker.js/jeju-router.js 주석 정정)** — 완료. `patch_20260711.diff`에 포함.
2. **§1 (패턴 G, `_isEmergency`)** — ⚠ **계획 자체가 틀렸었습니다.** 실행 중 재확인한 결과
   `_isEmergency`는 죽은 참조가 아니라 **`Openhash-Gopang/jeju` 저장소에 실제로 존재하는
   정규식 하드 게이트**였습니다(`EMERGENCY_RE`, 다른 모든 라우팅보다 먼저 실행). 지난
   세션도, 이번 계획서 초안도 **같은 원인(모노레포 범위로만 grep)으로 두 번째 오진**을
   낸 것입니다. 대신 실제 발견한 문제는: `call-ai.js`의 `kemergency.triggers` 배열과
   `jeju-router.js`의 `EMERGENCY_RE`가 **서로 다른 파일에 독립적으로 존재하는, 부분적으로만
   겹치는 키워드 세트**라는 drift 위험입니다(예: `EMERGENCY_RE`엔 '자살'·'납치'·'스토킹'이
   있는데 `triggers`엔 없음). 주석을 정정하고 이 drift를 명시적으로 기록해 뒀습니다
   — 통합 여부는 정책 결정 필요.
3. **§2 (K서비스 배선 점검)** — 완료. `tools/check_gov_relay_wiring.py` 작성·실행,
   `docs/gov_relay_wiring_audit_20260711.md`에 해석 포함 결과 정리. **실제 결함 2건 발견**:
   `school`(kedu), `stock`(kfinance) — 서버가 이미 `UNIVERSAL_FORCED_K_SERVICES`에
   등록해 둔 자동 안전장치가 있는데도 `/deepseek`이 아니라 무방비 `/ai/chat`을 호출해
   그 혜택을 못 받고 있음. `market`/`gdc`/`security`는 애초에 `GOV_AGENCIES` 소속이
   아니라 `/deepseek` 직접 호출이 정상 설계임을 확인(자동 스크립트가 NO_RELAY로
   표시했지만 오탐).
4. **jejudo 저장소** — `LICENSE` 파일만 있는 빈 저장소로 확인. 정리는 주피터님 판단.

## 다음 세션 우선순위 (갱신)

1. ★ `school`, `stock` 저장소(별도 저장소, 이번 세션 범위 밖)를 열어 `/ai/chat` →
   `/deepseek`(+`service_id`) 교체 — `docs/gov_relay_wiring_audit_20260711.md`의
   권고 코드 참고.
2. kemergency triggers ↔ EMERGENCY_RE 키워드 통합 여부 정책 결정.
3. 인수인계 문서 §2-1(K-Search RULE-07 명문화), §2-3(gwp_registry curl 검증) — 여전히 미착수.
4. traffic/desktop.html, klaw/benchmark.html의 `/ai/chat` 직접호출 — 낮은 우선순위로 정리.

---

# (아래는 원래 계획 원문 — 그대로 보존)


대상 저장소: `Openhash-Gopang/gopang` HEAD `646f5b5` (지난 세션과 동일, 변동 없음).
이 계획서는 지난 세션의 `패턴BCDGHI_코드대조검증_20260711.md`를 **대체하지 않고 정정**합니다.

---

## 0. ⚠ 최우선 정정 사항 — 지난 세션의 "패턴 B 🔴 심각" 진단은 오진(誤診)이었습니다

**지난 세션이 확인한 파일**: `gopang` 모노레포 안의 `src/gopang/ai/jeju-router.js` +
`worker.js:4301` 주석("현재 jeju-router.js는 /gov/relay가 아니라 /ai/chat을 직접 호출").
이 근거로 "jeju 실제 트래픽에 위임 로직이 안 걸릴 가능성이 높다"고 결론 냈습니다.

**실제로 jeju.hondi.net을 서빙하는 파일**: 별도 독립 저장소 `Openhash-Gopang/jeju`의
`jeju-router.js`(이름은 같지만 완전히 다른 파일 — gopang 쪽 자체 주석이 2026-07-09에 이미
이 혼동을 명시적으로 경고해 둠). 이 파일을 직접 클론해 확인한 결과:

- `jeju` 저장소의 `webapp.html`은 **2026-07-05부로 이미 `/ai/chat` → `/gov/relay`로 마이그레이션
  완료**(`_govRelayCompletion()` 함수, 커밋 `ba1a086` 이전 시점).
- `worker.js`의 `handleGovRelay()`는 `jeju_do`/`jeju_national`을 `GOV_AGENCIES`,
  `NO_IDENTITY_LAYER_AGENCIES`, `SP_DELEGATION_ORIGINATORS`에 전부 정상 등록해 처리하고 있음
  (UNIVERSAL-INTEGRITY/UNIVERSAL-common 강제 주입, 위임 로직 포함) — 실사 코드 대조로 확인.
- 즉 **패턴 B는 정상 작동 중**입니다. 실제로 고장 난 것은 `worker.js:4301` 줄의 **주석이
  최신화가 안 된 것**뿐입니다(2026-07-05 마이그레이션 이후 이 주석만 갱신 안 됨).

**원인 분석**: 지난 세션이 "gopang 모노레포 = 전체 시스템"이라고 가정하고 그 안에서만
grep했습니다. 실제로는 프론트엔드가 22개 독립 저장소로 분리돼 있고, 그중 하나(`jeju`)가
백엔드(`worker.js`, 이것도 gopang 모노레포 소속)를 원격으로 호출하는 구조라, **grep 범위를
모노레포 하나로 한정하면 프론트엔드 쪽 최신화를 놓칩니다.** 이건 이번 세션들의 핵심 원칙
("문서보다 실제 코드를 먼저 grep한다")을 한 단계 더 정밀화해야 한다는 뜻입니다 — "실제 코드"는
gopang 모노레포뿐 아니라 **해당 서비스의 실제 배포 저장소**까지 포함해야 합니다.

### 0-1. 즉시 처리할 것 (5분 작업)
- `worker.js:4301~4303` 주석을 "2026-07-05 마이그레이션 완료, 현재 /gov/relay 정상 호출 중"으로
  갱신.
- `src/gopang/ai/jeju-router.js` 상단 주석(2026-07-09자)도 "아직 마이그레이션 전이다"라는
  표현이 있으면 "완료됨"으로 정정.
- `패턴BCDGHI_코드대조검증_20260711.md`에 정정 각주 추가(문서를 지우지 말고, 오진이었다는
  사실 자체를 기록으로 남기는 편이 낫습니다 — 같은 실수 재발 방지용 사례).

---

## 1. 패턴 G(§0-G 응급 게이트) — 지난 세션 진단 유지 (재확인 완료, 실제 결함)

`_isEmergency`라는 함수는 전체 git 히스토리(1,446개 커밋) 어디에도 정의된 적이 없고, 오직
주석 2곳(`call-ai.js:1728, 1772`)에서만 "jeju-router.js의 _isEmergency가 최우선 처리한다"고
참조됩니다 — 이 참조 자체가 죽은 참조(허구)임을 재확인했습니다. 실제 응급 처리는
`getService('kemergency')`의 트리거 배열을 재사용한 **점수 기반 매칭**(`call-ai.js` 1770행대)
으로 이뤄지고 있고, 이는 코드로 실재합니다 — 다만 "예외 없이 최우선"이라는 §0-G 원칙을
문자 그대로 보장하는 하드 게이트는 아니고 스코어링 우선순위일 뿐입니다.

### 처리 방안
1. `call-ai.js` 1728·1772행의 `_isEmergency` 참조 주석을 실제 구현("kemergency 트리거
   재사용 스코어링, getService 기반")으로 정정.
2. §0-G가 "예외 없이 최우선"을 요구한다면, 현재 스코어링 방식이 그 요구를 실제로 만족하는지
   테스트 케이스로 검증(예: 응급 키워드 + 다른 고득점 서비스가 동시에 걸리는 입력을 넣어
   kemergency가 항상 이기는지 회귀 테스트 추가).
3. 하드 게이트가 필요하다고 판단되면 별도 코드 레벨 사전 필터를 신설할지, 현재의 스코어링
   우선순위로 충분하다고 결론 내릴지 — 이건 정책 결정이 필요합니다(주피터님 확인 필요).

---

## 2. 패턴 C(K서비스) 통합 방안 — 22개 저장소 재조사 결과

조직 저장소 전체 목록을 확인한 결과 **28개**(K서비스 성격 저장소는 그중 약 20개)입니다.
5개(`health`, `tax`, `public`, `jeju`, `jejudo`)를 표본 확인한 결과:

| 저장소 | 실제 호출 경로 | 상태 |
|---|---|---|
| `tax` | `/gov/relay`(무료) + BYOK(사용자 키) | ✅ 정상 — 모범 사례 |
| `health` | `/gov/relay`(무료) + BYOK | ✅ 정상 |
| `public` | `/gov/relay`(무료) + BYOK | ✅ 정상 |
| `jeju` | `/gov/relay`(§0 정정 참고) | ✅ 정상(재확인) |
| `jejudo` | 없음 — `LICENSE` 파일만 존재 | ⚠ 빈 저장소, 용도 불명 |

`tax`/`health`/`public`/`jeju` 4개는 동일한 아키텍처 패턴(서버 강제 공통규칙 주입 +
BYOK 폴백)을 공유하고 있어, **이미 사실상 표준화가 이뤄진 상태**입니다. 지난 세션이 우려한
"K서비스마다 제각각"이라는 가설은 최소 이 4곳에서는 기각됩니다.

### 통합 방안 제안
1. `tax_repo/webapp.html`의 330~480행대 패턴("서버 강제 상속 릴레이" 블록)을 **공식 레퍼런스
   템플릿**으로 지정하고, 나머지 K서비스 저장소(약 15개 미확인)를 이 템플릿과 diff하는 자동
   점검 스크립트를 작성(`tools/check_gov_relay_wiring.py` — 각 저장소를 얕은 클론해
   `/gov/relay` 존재·BYOK 폴백 존재·`/ai/chat` 직접호출 잔존 여부를 자동 채점).
2. `jejudo` 저장소는 용도가 불명확합니다 — 사용 안 하는 빈 저장소라면 조직 정리 대상으로
   기록만 해두고(삭제는 주피터님 판단), 실수로 잘못 만든 이름이라면 `jeju`와의 혼동을
   방지하기 위해 archive 처리를 권합니다.
3. 미확인 15개 저장소(`market`, `gdc`, `security`, `insurance`, `school`, `traffic`,
   `logistics`, `stock`, `democracy`, `police`, `911`, `users`, `qna`, `klaw`,
   `openhash-*` 5종)를 1번 스크립트로 일괄 점검 — 이번 세션에서 4개 표본은 전부 정상이었으나,
   "전부 정상일 것"이라고 추정하지 않고 실제로 돌려서 확인하는 것이 원칙에 맞습니다.

---

## 3. 인수인계 문서(§2)에 남아 있던 미해결 항목 — 여전히 유효

- **§2-1**: K-Search(SP-18) STEP 3-C가 RULE-07(웹검색 폴백) 경로를 SP 텍스트에 명시 안 함.
  → SP-18 v1.4로 갱신 필요 (미착수, 유효).
- **§2-3**: `gwp_registry`의 `~`(LIKE) PocketBase 필터 문법을 실제 curl로 검증 안 함
  (미착수, 유효).

---

## 4. 이번 세션 작업 순서 제안

1. §0-1 정정 커밋(주석 2곳 수정) — 가장 저비용·고가치, 먼저 처리.
2. §1 `_isEmergency` 죽은 참조 주석 정정 + kemergency 우선순위 회귀 테스트 1건 추가.
3. §2 `tools/check_gov_relay_wiring.py` 작성 → 22개 저장소 일괄 점검 실행 → 결과 반영.
4. 위 3개 완료 후 §3(K-Search RULE-07 명문화, gwp_registry curl 검증)로 진행할지,
   여기서 다음 세션으로 넘길지 주피터님께 확인.

---

## 커밋 방법 (주피터님 로컬)

```powershell
cd "$env:USERPROFILE\Downloads\gopang"
Move-Item -Force "$env:USERPROFILE\Downloads\작업계획서_20260711.md" "docs\작업계획서_20260711.md"
git add docs\작업계획서_20260711.md
git commit -m "docs: 패턴 B 오진 정정 + 다음 단계 작업계획서"
git pull origin main --rebase
git push origin main
```
