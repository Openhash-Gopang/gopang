# 작업 지시서 — 2026-08-09 세션 인수인계

주피터님 지시 원문 요약: 국가기관 지역사무소 전국 확장(제주 원형 →
15개 도 인스턴스화), 라우팅 계층 검증(kgov/kregionalgov/K서비스 분기),
특정 기관명 우선 원칙 신설, 그리고 마지막으로 "새 대화창에서 호출된
제주 지역 공공기관·부서 SP가 관제탑 원칙에 따라 대응하는지" 테스트
착수 지시.

## 0. 먼저 확인할 것

이 세션에서 병합된 PR은 #275~#286(12개)이다. main HEAD: `03f1074`.
다음 세션 시작 시 `git log --oneline -15`로 그 이후 다른 세션이 추가
커밋을 얹지 않았는지 먼저 확인할 것(동시 세션 다수 활동 중인 저장소).

## 1. 이번 세션에서 완료한 것

### 1-1. 인계서(20260808) 이어받아 4개 PR 병합 (#275~#278)
province-aware SP 저장소 라우팅 인프라, 부산 중복 레코드 제거, 15개 도
국가기관 라우팅 일반화, 관할구역 리서치 스테이징 데이터. 순서대로
#276→#275→#277→#278.

### 1-2. CI 기존 실패 정리 (#279)
`check-stale-refs`(35건)·`check-hardcoded-sp-refs`(12건) — 이번 세션
PR과 무관하게 main에 이미 있던 부채였음을 로컬 재현으로 확인 후 정리.

### 1-3. 국가기관 데이터 Phase A/B (#280, #281)
`national-agency-master-data.json` 47건 → **151건**.
- Phase A(#280): 도코드당 지사가 정확히 1건인 경우만(stat/coastguard/
  police/court/prosecution) 51건 통합
- Phase B(#281): mma/veterans/weather/port 재검증(namu.wiki·law.go.kr
  원문 대조), 42건 추가
- 원칙: `_fetchNatText()`가 domain+도코드로 `.find()`하는 구조라, 도
  하나에 지사가 여럿인 곳(tax 전체, court/police/veterans/port의
  seoul·gyeonggi 등)은 이번에도 **의도적으로 보류** — 틀린 지사를
  확정해 말하는 위험(2026-08-08 완도해양경찰서 오매칭 사례) 때문.

### 1-4. 시/군 단위 다중 지사 분기 인프라 (#282)
`_fetchNatText(entry, 시코드)`로 확장, 선택 필드 `시코드목록` 신설.
도코드 레코드가 2건 이상이면 시코드목록으로 좁히고, 그래도 못 정하면
**정직하게 되묻는다**(`_NAT_MULTI_BRANCH_PROMPT`) — 오답 대신. Seoul
5개 법원/검찰청을 구(區) 단위로 실사 등록해 검증(25개 구 전부 커버
확인). 기존 도코드당 1건 도메인은 회귀 없음.
**아직 미해결**: gyeonggi 법원(수원/의정부) 등 원자재가 시/군 단위까지
불완전한 나머지 다중 지사 도메인.

### 1-5. AC 위치 자체 확보 (#283)
`pages/regional-gov.html`이 메인 비서가 건네는 URL facts 없이도(북마크·
직접 접속) `services/location.js`(GPS→Kakao 역지오코딩)로 스스로 위치를
확보하도록 연결. `_resolveLocationHint()`가 매 턴 새로 읽음(1회 캐시
아님). 지갑 서명이 필요한 프로필 주소(PDV) 우선 조회는 이 페이지가
지갑 부트스트랩을 안 해서 미연결 상태로 남음(후속 과제).

### 1-6. 라이브 스모크테스트 인프라 신설 (#284)
`tests/live_smoketest/live-national-agency-smoketest.mjs` — 39건
(국가기관 HIT/FALLBACK 30 + 시군분기 6 + AC위치전달 3), 전부 PASS.
gov-router.js는 키워드+시/군 매칭만으로 결정론적이라 LLM
없이(`raw.githubusercontent.com`만으로) 완결.

### 1-7. kgov/kregionalgov/기타 분기 실측 + 규칙 수정 (#285, #286)
**중요 — 이 세션 최대 발견**: `assembleGovSystemPrompt()` 단독 테스트는
전체 AC 파이프라인의 일부(kregionalgov 내부 라우팅)만 검증한다. 실제
사용자 발화는 AC-PRO-CORE(§CORE 주체 5단계: ①K시리즈→②정부기관→
③EXPERT→④비즈니스→⑤직접응답)의 LLM 판단을 먼저 거친다 — 이 최상위
단계는 **DeepSeek 실제 호출이 필요해 sandbox에서 직접 테스트 불가**,
기존 `live-smoketest.yml`(GitHub Actions, `DEEPSEEK_API_KEY` 시크릿
보유, `workflow_dispatch`로 임의 시나리오 파일 지정 가능)로 우회 실행.

30건 라이브 실측(`scenarios_kgov_vs_kregionalgov_20260809.json`)에서
"대구 중부경찰서"·"제주세무서"처럼 **특정 기관명을 언급해도** 그 화제와
겹치는 일반 K서비스(kpolice/ktax)가 5단계 순서상 먼저 걸려 kregionalgov
를 건너뛰는 문제를 실측 확인. **AC-PRO-CORE_v1_5.txt → v1_6.txt**로
버전 상향(이 저장소 관례 — 내용 개정 시 파일명 버전 상향)하며 예외
규칙 신설: 특정 기관명이 직접 언급되면 5단계 순서보다 먼저 그 기관
자체를 호출하는 경로(kregionalgov 또는 §GOV_MATCH)를 최우선 시도.
재실측으로 핵심 사례(#14, #16, #18) 전부 수정 확인.

**PAT workflow 스코프 관련**: `.github/workflows/*.yml` 수정은 GitHub이
별도 권한(fine-grained PAT의 "Workflows: Read and write")을 요구한다.
처음엔 이 권한이 없어 막혔다가, 주피터님이 토큰에 권한을 추가해주신
뒤 정상 처리됨(Cloudflare 시크릿과는 무관 — GitHub 토큰 쪽 설정임을
헷갈리지 말 것).

### 1-8. 제주 공공기관 전수 커버리지 테스트 (미병합, 스크래치 결과만 남음)
`scenarios_jeju_full_coverage_20260809.json`(110건, 국가기관 지사 34+
도청 L2 18+시청 국 14+행정시 2+직속기관 10+출자출연기관 26+읍면동 6)
+ `scenarios_jeju_unnamed_20260809.json`(58건, 기관명 미언급 상황서술
버전) + `scenarios_jeju_unnamed_with_location_20260809.json`(같은 58건
+ `[현재 위치]` 컨텍스트 주입) — **이 3개 파일은 브랜치에만 있고 PR로
병합하지 않았다**(테스트 목적 산출물, 영구 회귀 스위트로 남길지는
다음 세션 판단 필요). 결과 요약:
- 기관명 명시 110건: 76 직접 PASS + 32 CALL_KINTENT(§GOV_MATCH 정상
  경로, 채점기가 FAIL로 오판) + 2 진짜 오배정(#13 난대·아열대산림
  연구소→kgov, #15 국립제주검역소→khealth)
- 기관명 미언급+위치 없음 58건: 1 PASS + 23 CALL_KINTENT + 34 다른
  K서비스로 감 — **불공정한 테스트였음**(아래 참조)
- 기관명 미언급+`[현재 위치]` 주입 58건: 17 PASS + 22 CALL_KINTENT +
  19 다른 K서비스(대부분 합리적 — 일반 국가사무 질문은 K서비스가
  바로 답해도 무방) — 단 **#3 "고소장 접수 어디로 가야 하나요"**는
  명시적 위치질문인데도 kpolice가 "제주동부경찰서 등"이라는 **없는
  지사명을 즉석 추측**해 답함(정직한 정보없음 원칙 위반 가능성,
  미조사 상태로 남김).

**핵심 교훈(다음 세션도 반드시 지킬 것)**:
1. `live_smoketest.py`의 `call_deepseek()`는 위치 컨텍스트를 전혀 안
   넣는다 — 실제 프로덕션은 `_buildLocNote()` 형식(`\n\n[현재 위치]\n
   {주소}`)을 사용자 턴에 반드시 붙인다. 위치 의존적 시나리오는 이걸
   수동으로 발화 끝에 붙여야 공정한 테스트가 된다.
2. `[CALL_KINTENT: query=...]`는 실패가 아니라 §GOV_MATCH의 정상 경로
   중 하나다 — `expected_id: kregionalgov` 단일 기대값으로 채점하면
   진짜 성공률을 과소평가한다. raw_response를 직접 열어 CALL_KINTENT
   여부를 구분할 것.
3. 발화에 기관명을 직접 넣은 시나리오는 "텍스트 매칭"만 검증한다 —
   "상황 서술만으로 추론"을 검증하려면 반드시 기관명 없는 짝을 만들어
   비교해야 한다.

## 2. 다음 세션 작업 — 주피터님 최신 지시

**"새 대화창에서 호출된 제주 지역 공공 기관 및 부서 SP가 관제탑
원칙에 따라 대응하는지 테스트"**

### 2-1. 이게 지금까지 테스트와 무엇이 다른가
지금까지는 **라우팅**(맞는 기관을 찾아 연결하는가)만 검증했다. 이번엔
**그 기관 SP 자체의 응답 스타일**(연결된 뒤 실제로 대화하는 방식이
`CONTROL-TOWER-PRINCIPLE_v1_1.md` 원칙을 지키는가)을 검증해야 한다 —
완전히 다른 층이다.

### 2-2. 관제탑 원칙 핵심(prompts/CONTROL-TOWER-PRINCIPLE_v1_1.md)
- **설명(지양)**: 백과사전식 나열, 사용자 고유 입력값을 지워도 그대로
  성립하는 답변, 필요서류·유의사항 한 번에 나열
- **실행 관철(지향)**: 사용자의 구체적 입력을 반영한, 다음에 할 일
  하나로 좁혀진 응답. 마크다운 제목·번호목록·불릿 금지, 1~3문장.
- STEP A류(감별진단·쟁점추출 등) 산출물은 내부 추론 구조일 뿐 — 그대로
  사용자에게 쏟아내면 위반.
- "하나의 동작" ≠ "기계적으로 한 문장에 한 조작" — 사용자가 중간
  보고 없이 한 호흡에 할 수 있는 연속 조작(로그인→검색→기입)은 묶어도
  됨. 턴을 실제로 나눠야 하는 경계는 (a)물리적 완료 후 결과 확인 필요
  지점, (b)혼디 판단에 사용자 답이 필요한 분기점뿐.
- 예외: 요청이 불분명해 되묻는 중인 턴, 인간 전속 경계 고지 정형 블록.

### 2-3. 테스트 설계 시 주의할 것(이번 세션 교훈 재적용)
- **"새 대화창에서 호출된"이라는 조건이 핵심이다** — `directCode`
  경로(§GOV_MATCH가 이미 기관을 특정한 뒤 그 SP를 첫 시스템 프롬프트로
  까는 경우, `worker.js`의 `handleGovTreeStepExecute` 참고)로 시작해야
  진짜 "그 기관 SP가 관제탑 원칙을 지키는가"를 본다 — 매번 라우팅부터
  다시 태우면 이번 세션이 이미 검증한 걸 반복하는 것일 뿐이다.
- `assembleGovSystemPrompt(text, null, null, null, directCode)`로
  실제 기관 SP 텍스트(systemPrompt)를 얻은 뒤, 그걸 시스템 프롬프트로
  삼아 **실제 사용자 민원 발화**를 1턴 또는 멀티턴으로 넣어 DeepSeek을
  호출해야 한다 — 이것도 `live-smoketest.yml`/`live_smoketest.py`
  패턴을 그대로 재사용할 수 있는지, 아니면 시스템 프롬프트를 매
  시나리오마다 다르게 넣어야 해서(SP마다 다름) 새 하네스가 필요한지
  먼저 확인할 것(`live_smoketest.py`는 `--system-prompt` 인자 하나를
  전체 실행에 고정하는 구조라, 시나리오별로 다른 SP를 로드하려면
  스크립트 수정이나 새 스크립트가 필요할 가능성이 높음).
- 채점 기준: 태그 매칭(`[GWP:]` 등)이 아니라 **형식 위반 여부**(마크다운
  헤딩·번호목록 사용, 3문장 초과, 사용자 입력값 없이도 성립하는 문장
  포함 여부)를 정규식/휴리스틱으로 1차 자동판정하고, 애매하면
  `live_smoketest.py`의 `LIVE-NEEDS-REVIEW` 관례처럼 사람 검토로
  넘길 것 — 이번 세션에서도 자동채점 오판(CALL_KINTENT를 FAIL로 오판)
  이 실제로 있었으니 자동판정 결과를 그대로 믿지 말고 raw_response를
  반드시 직접 대조할 것.
- 대상 후보: 오늘 만든 제주 34개 국가기관 지사 + 18개 도청 L2 + 14개
  시청 국 + 10개 직속기관 + 26개 출자출연기관 중, 관제탑 원칙 위반이
  실제로 자주 보고됐던 유형(백과사전식 나열 위험이 큰 것 — 예: 세무서
  "종합소득세 신고 절차 알려줘"류, 법원 "소송 절차가 궁금해요"류)부터
  우선 검증하는 게 효율적일 것.

### 2-4. 남겨둔 미해결 항목(우선순위순, 이번 세션에서 새로 발견)
1. `#3 "고소장 접수 어디로 가야 하나요"` — kpolice가 존재하지 않을 수
   있는 지사명("제주동부경찰서 등")을 즉석 추측해 답한 사례. 정직한
   정보없음 원칙 위반인지 재확인 필요.
2. `#13`(난대·아열대산림연구소→kgov), `#15`(국립제주검역소→khealth) —
   인지도 낮은 국가기관 2곳이 v1.6 "특정 기관명 우선" 규칙에서 빠짐.
3. tax/customs 전체와 court·police·veterans·port의 다중 지사 도(seoul,
   gyeonggi, gangwon, gyeongnam, jeonnam-gwangju 등) — 시/군 분기
   인프라(#282)는 있으니 원자재만 확보하면 통합 가능.
4. `scenarios_jeju_full_coverage_20260809.json` 등 3개 스크래치 시나리오
   파일을 영구 회귀 스위트로 병합할지 결정 필요(현재 미병합 브랜치에만
   존재 — `feature/jeju-full-coverage-scenarios`,
   `feature/jeju-unnamed-coverage-scenarios`,
   `feature/jeju-unnamed-with-location-scenarios`).

## 3. 재발 방지 — 이번 세션에서 배운 것

- **테스트 대상 계층을 정확히 특정할 것** — gov-router.js 단독 테스트와
  AC-PRO-CORE 전체 파이프라인 테스트는 완전히 다른 걸 검증한다. 어느
  계층을 테스트하는지 먼저 명확히 하고 시작할 것.
- **자동 채점기를 맹신하지 말 것** — `CALL_KINTENT`를 FAIL로 오판한
  사례가 이번 세션에 최소 2번 있었다. raw_response 원문 대조 없이
  숫자만 보고 결론 내리지 말 것.
- **위치 의존적 판단 규칙을 테스트할 땐 위치 컨텍스트를 반드시 넣을
  것** — 안 넣으면 불공정한 테스트가 되고 가짜 실패가 대량 발생한다.
- **git 워크플로**: push 전 항상 pull/rebase, feature 브랜치→PR→병합
  (main 직접 푸시 금지), 3개 이상 파일 배포 시 zip, `.github/workflows/`
  수정은 PAT에 Workflows 권한 필요(Cloudflare 시크릿과 무관).
