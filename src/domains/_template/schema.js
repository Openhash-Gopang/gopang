/**
 * @file schema.js (_template)
 * @description 데이터 스키마 — TODO: 도메인 전용 필드 정의
 */

export const dataSchema = {
  // vault.js MessageStore 확장 필드
  messageRecord: {
    // 예: domainSpecificField: { type: 'string', default: '' }
  },
  // 보고서 스키마
  reportRecord: {
    // 예: verdict: { type: 'string' }
  },
}
