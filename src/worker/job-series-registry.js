// job-series-registry.js
// ---------------------------------------------------------------------------
// access_cert의 신규 필드 job_series(직류/계급) 화이트리스트.
// national-role-registry.js(직위명, 국장급 이상)와는 다른 분류축이다 —
// job_series는 "이 사람이 어떤 직무 계열/계급인가"(예: 사회복지 직류,
// 경위 계급)를 나타내며, EMERGENCY_ELIGIBLE_JOB_SERIES(worker.js)가
// 이 필드를 검증 대상으로 삼는다.
//
// 반입일: 2026-07-15 / 출처: code.go.kr 분류별 코드검색(인사정책 및
// 인적자원관리 분류)에서 직접 조회한 "직류"·"계급" 코드종의 실제 옵션 값.
// NATIONAL-OFFICIAL-ROLE-REGISTRY_v1.0 §3 원칙("지어내지 않는다")에 따라,
// 실제로 조회 확인된 값만 담았다 — 아동보호 관련은 별도의 독립 국가 직렬
// 코드를 code.go.kr에서 찾지 못해(아동보호전문요원은 통상 민간위탁기관
// 종사자이거나 사회복지직 겸임) 포함하지 않았다. 필요해지면 별도 확인 후
// 추가할 것 — 사회복지 직류로 임시 대체하지 않는다(겸임이 항상 성립한다는
// 보장이 없으므로).
export const JOB_SERIES_REGISTRY_DATA = [
  // 인사정책 및 인적자원관리 > 직류 (사회복지 관련만 발췌, 전체 목록은
  // code.go.kr 분류별 코드검색 > 직류 참고 — 전체 반입은 별도 과제)
  '사회복지',
  '사회복지전문',
  // 인사정책 및 인적자원관리 > 계급 (경찰 계급 전체)
  '순경', '경장', '경사', '경위', '경감', '경정', '총경',
  '경무관', '치안감', '치안정감', '치안총감',
];
